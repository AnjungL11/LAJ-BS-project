# BACKEND
## 后端运行
### 方法一
打开 `backend/src/main/java/com/imagemanager/ImageManagerApplication/java` 文件，点击灰色 `Run | Debug` 字样的 `Run` 即可运行后端。

### 方法二
先进入后端 `backend` 目录。
```powershell
cd backend
```
然后运行如下命令启动后端。
```powershell
mvn spring-boot:run
```